# Copy to `.env` and set the LAN-reachable IP for your gateway host.
LAN_IP=192.168.1.100

# Optional comma-separated list of API keys required by the dashboard endpoints
# Example: DASHBOARD_API_KEYS=not-needed,secondary-key
DASHBOARD_API_KEYS=
# Alternative singular key (falls back to the first entry in DASHBOARD_API_KEYS if omitted)
# DASHBOARD_API_KEY=

# Override defaults if needed:
AIHUB_IP=${LAN_IP}
GATEWAY_PORT=8080

# Monitoring configuration (optional overrides)
# Absolute path to the nginx JSON access log mounted into the dashboard container
NGINX_ACCESS_LOG=/var/log/nginx/access.log
# Maximum number of lines scanned per refresh
MONITORING_MAX_SCAN=10000
# Default number of events returned by /api/monitoring/events
MONITORING_DEFAULT_LIMIT=200
# Rolling window (minutes) for alert heuristics
MONITORING_ALERT_WINDOW_MIN=60
# Alert thresholds
MONITORING_CLIENT_ERROR_THRESHOLD=12
MONITORING_RATE_THRESHOLD=120
MONITORING_MISSING_KEY_THRESHOLD=25
# SQLite persistence (optional overrides)
# MONITORING_DB_PATH=/app/data/monitoring.sqlite3
# MONITORING_STATE_PATH=/app/data/monitoring_state.json
# MONITORING_MAX_AGE_DAYS=30
# OpenRouter gateway defaults (set when enabling the OpenRouter relay)
OPENROUTER_API_KEY=""
OPENROUTER_MODEL=minimax/minimax-m2:free
